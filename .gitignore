__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.env

# state/ НЕ игнорируется — это и есть хранилище состояния, оно обязано
# быть в git. Игнорируем только рабочие артефакты внутри него.
state/*.tmp
