"""Наблюдатель за howborisusesclaudecode.com.

Устройство пакета (снизу вверх, каждый слой не знает о вышестоящих):

    config.py   конфиг из TOML + секреты из окружения
    source.py   сеть и HTML: условный GET, нормализация, парсинг в структуру
    state.py    снапшот / журнал доставок / heartbeat + запись в git
    detect.py   инварианты и диф: структура -> список событий
    analyze.py  событие -> контекст -> модель -> валидированный разбор
    deliver.py  разбор -> текст -> Telegram
    main.py     оркестрация и правила продвижения состояния

Сквозной инвариант всей системы:
    снапшот продвигается вперёд ровно тогда, когда ВСЕ события прогона
    подтверждённо доставлены. Во всех остальных случаях он остаётся
    прежним, и следующий прогон пересчитает диф заново.
"""

__version__ = "1.0.0"
