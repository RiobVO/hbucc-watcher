# Промт для новой сессии

Скопируй всё, что ниже двойной черты, в новый чат Claude Code, открытый в `E:\SCRIPT`.

---
---

## Контекст

В `E:\SCRIPT` — наблюдатель за сайтом https://howborisusesclaudecode.com/. Когда там появляется новое, он присылает мне в Telegram готовый разбор на русском: что это, как работает по сути, работает ли на Windows, что делать и стоит ли моего времени. Смысл в том, чтобы я не заходил на сайт вообще. Репозиторий приватный: https://github.com/RiobVO/hbucc-watcher

**Система задеплоена и работает.** GitHub Actions, cron раз в шесть часов, секреты залиты, базовая линия записана, снапшот лежит в ветке. Прогоны идут зелёными.

Прочитай `README.md` — там архитектура, два инварианта, раскладка по файлам и раздел «Статус проверки». **Не переделывай проект и не предлагай альтернативную архитектуру.** Она обсуждена, обоснована, реализована и проверена исполнением.

## Обо мне

Соло-бэкенд-разработчик, Ташкент. Python, aiogram, FastAPI, PostgreSQL, Next.js. **Работаю на Windows** — консоль в cp1251, для разовых скриптов ставь `PYTHONIOENCODING=utf-8`, иначе русский вывод падает с `UnicodeEncodeError`.

## Что уже доказано исполнением — не перепроверяй

Раздел «Проверено исполнением» в `README.md` — источник истины. Коротко: загрузка и парсинг, диф во всех пяти случаях, инварианты целостности, состояние и запись в git включая расхождение веток, граница доверия, вызов модели с полным набором параметров, разбор реального события, доставка в Telegram включая разрыв внутри сворачиваемого блока, полный цикл с обоими инвариантами натурно, оба прогона в CI, пинг сторожу, проба доступности модели по расписанию. Тестов 153.

Если сомневаешься, проверено ли что-то, — смотри в README, а не переделывай.

## Что осталось

По убыванию важности.

### 1. Первое настоящее изменение на сайте

Единственное, чего нельзя устроить по своей воле. Все части пути проверены по отдельности, цикл проверен целиком — но на подставном изменении. Как поведёт себя диф на том, что сайт сделает на самом деле, неизвестно. Когда придёт первый настоящий разбор — посмотреть на него внимательно: сошёлся ли тип события, не поехал ли порог сопоставления, читается ли текст.

### 2. Страницы разборов: сделаны руками, в конвейере их нет

Это главный незакрытый кусок. Сейчас `deliver.py` шлёт в Telegram полное сообщение на 5–7 тысяч символов со сворачиваемыми блоками. Решено делать иначе: **страница + короткая карточка со ссылкой**.

Уже сделано руками и работает:

- публичный репозиторий `github.com/RiobVO/hbucc-reports`, GitHub Pages включены, две страницы отдают HTTP 200;
- вёрстка одобрена — стиль Claude Code Insights: slate-палитра, Inter, ряд метрик, янтарный блок «Коротко», карточка автора, слои карточками, расхождения отдельным блоком, код с кнопкой «копировать». **Образец брать из уже опубликованных страниц** — они и есть эталон вёрстки: `2026-07-30-postcompact.html` и `2026-07-30-domain-knowledge.html` в репозитории отчётов;
- карточка в Telegram на 400–600 символов: заголовок, вердикт, Windows, что сделать, число расхождений и ссылка.

Чего нет в коде:

```
watcher/publish.py      рендер страницы из Analysis + публикация в репозиторий отчётов
templates/report.css    вёрстка отдельно от кода — менять вид без правки Python
watcher/deliver.py      короткая карточка вместо полного текста
watcher/main.py         связать: сначала публикация, потом карточка
config.toml             адрес репозитория отчётов, базовый URL страниц, выключатель
tools/setup.py          пятый секрет REPORTS_TOKEN
.github/workflows/      прокинуть секрет в прогон
tests/test_publish.py   рендер, экранирование, деградация
```

Два требования, без которых делать нельзя:

1. **Публикация обязана деградировать.** Не опубликовалось — уходит полный текст, как сейчас, и в журнал пишется, что страницы нет. Карточка со ссылкой на 404 хуже простыни.
2. **Публикация до отправки.** Сначала страница живёт по адресу, потом читатель получает ссылку.

Токен: `GITHUB_TOKEN` в Actions имеет права только на свой репозиторий, поэтому для записи в `hbucc-reports` нужен отдельный PAT с правом на этот репозиторий.

Ещё не сделано: индексная страница со списком разборов — иначе архив не найти.

### 3. Ревью Codex: диф от 30 июля прошёл, переезд провайдера — нет

Диф от 30 июля (поле `where_it_fits`, проба доступности модели) отревьюен: две блокирующие находки, обе подтвердились и исправлены — временные отказы API считались приговором аккаунту, и проба ломала коммит heartbeat. Подробности в «Проверено исполнением».

Не отревьюен более ранний переезд провайдера: белый список доменов переехал из API провайдера в наш код (`watcher/original.py`) — это граница безопасности, а `CLAUDE.md` для такого требует независимого чтения. Диф давно в `main`, ревью читающее и ничего не ломает.

### 4. Part 22 и Part 7 разобраны не до конца

Шесть советов подавались в конвейер руками (часть попала в базовую линию, поэтому диф её не выдаст), дошло три-четыре. Скрипт писать заново: события `BLOCK_ADDED` из `snapshot.json` через настоящие `analyze()` → `render()` → `deliver()`, состояние не трогать. Шесть разборов — $0.5–1.5 в зависимости от того, сколько модель решит искать.

## Секреты

Четыре: `OPENAI_API_KEY`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`, `HEALTHCHECK_URL`.

**Не проси присылать их в чат и не вставляй в командную строку** — argv виден в списке процессов и оседает в истории шелла, на этом стоит хук. Для локальных прогонов они лежат в `E:\SCRIPT\.env` (файл в `.gitignore`) — читай их кодом, не печатай значения. В GitHub — через `python tools/setup.py secrets`, ввод интерактивный.

Проверка всех четырёх: `python tools/setup.py check`.

## Грабли, на которые уже наступали

**Обнаружение**

- **`difflib.SequenceMatcher` и `autojunk`.** На последовательностях длиннее 200 элементов объявляет мусором всё, что встречается чаще чем в 1%. Сравнение идёт по **словам** и с `autojunk=False`. Единственное определение похожести — `detect.similarity()`; трогаешь матчинг — меняй только там и обязательно прогони `python tools/replay.py --sweep 22:1`. С включённым autojunk правка 5% слов давала ratio 0.36 вместо 0.95, и каждая реальная правка классифицировалась как удаление плюс добавление — при зелёных тестах.
- **`data-volume` не равен номеру части.** Тома 1–7 это Part 1–7, тома 8 и 9 — вообще не части (страницы установки скиллов), тома 10–24 это Part 8–22. Номер берётся только из подписи кнопки навигации.
- **Число советов в части сайт публикует сам** (`.volume-count`). Это сильнейший инвариант целостности, сильнее эвристических порогов.
- **Условный GET отвечает по-разному с разных машин.** 30 июля в 13:19 UTC раннер CI получил на сохранённый ETag 200 OK, а локальный прогон минутой позже — 304 Not Modified. Один и тот же тег, один документ; похоже на разные узлы CDN. Работу по-прежнему делает второй уровень каскада — хеш контента.

**Модель**

- **У модели нет инструмента загрузки страниц.** Первоисточники загружает `watcher/original.py` до вызова. Промт обязан говорить это прямо: пока он утверждал «открой пост», модель отчитывалась о действиях, которых не совершала.
- **У `web_search` OpenAI нет потолка на число вызовов.** Был у прежнего провайдера, здесь нет. Число поисков логируется на каждом разборе — это единственный способ заметить разгон.
- **`/v1/models` отдаёт 200 при нулевом балансе.** Поэтому проба (`analyze.probe_model`, её же зовёт `setup.py check`) делает ещё и крошечный запрос на генерацию и отличает `insufficient_quota`.
- **`web_search_context_size` — не рычаг стоимости.** Замер на одном и том же событии: `low` дал вход 22 812 токенов, `medium` — 22 782. Разницы нет. Счёт делает ЧИСЛО поисков, а его выбирает модель по материалу: от одного до шести, то есть от $0.08 до $0.27. Крутить эту ручку ради экономии бессмысленно.
- **Сайт ведёт @CarolinaCherry, а не Борис Черни.** Факт из `<meta name="author">` самой страницы; там же написано «not affiliated with Anthropic». Борис — тот, о чьих приёмах сайт рассказывает. Имя вытаскивает `source.site_author()`, оно попадает в контекст модели и в подписи слоёв.
- **Про проекты читателя модель не знает ничего.** Поле «где ляжет у тебя» это провоцировало: «в твоём скоринге на FastAPI заменит те три захода» — сочинение, которое звучит убедительно и не проверяется. Заменено на `example` с общим воспроизводимым примером.
- **Ленты X без платного API не читаются.** Проверено 30 июля: в HTML профиля ноль текстов твитов, `data-testid="tweet"` отсутствует, видимого текста 3 КБ из 250 КБ — остальное JS-бандл. Достаются только `og:title` (имя) и `og:description` (био) — на них и построен `fetch_author()`. Следить за лентами Карпаты и прочих можно либо через X API (~$200/мес), либо через RSS их блогов.
- **Три поля разбора ни разу не рассматривались глазами.** `how_to_verify`, `pitfalls`, `related` добавлены в схему и промт, живой разбор их заполнил и прошёл строгую валидацию — но вывод отладочного скрипта их не показал. Первым делом посмотреть на них в следующем разборе.
- **Модель врезает markdown-цитаты в текст** в виде `([домен](url))`. Промт это запрещает, `deliver.strip_citations()` страхует и переносит адреса в источники.

**Доставка**

- **Telegram отвергает разорванный тег целиком** — разбор просто не доходит, а в логе 400. Строчные теги в `render()` не пересекают перенос строки; `blockquote` многострочный, поэтому `chunk()` закрывает его перед разрывом и открывает заново вместе с атрибутом `expandable`.

**Прочее**

- **Порядок git-операций:** сначала `commit`, потом `pull --rebase`, потом `push`. Наоборот — «cannot pull with rebase: you have unstaged changes».
- **`bootstrap` глотает существующее содержимое.** Часть, попавшая в базовую линию, никогда не приедет разбором: для системы это уже известное. Разбирать такую часть — только подачей событий руками.
- **Не пропускай долгие фоновые прогоны через `grep`/`tail`** — конвейер буферизует, и до конца работы вывода не видно вообще.
- **Замена в файле через python-heredoc молча не срабатывает.** Если `str.replace` не нашёл строку, файл перезаписывается без изменений, а скрипт бодро рапортует об успехе. На этом уже потерян один живой разбор: правка в отладочный скрипт не встала, и три новых поля не попали в вывод. Ставь `assert old in s` перед каждой заменой.
- **Песочницы для зондов делай в свежем каталоге.** На Windows `rmtree` молча спотыкается о залоченные файлы git, и переиспользование одного пути роняет зонд через раз по причине, к предмету проверки не относящейся.

## Как работать

Правь по одному куску, каждый доводи до исполненного доказательства. Не «должно работать», а вывод команды. Тесты обязаны оставаться зелёными: `python -m pytest tests/ -q` — сейчас 153.

Стоимость реальная, аккаунт предоплатный. Прежде чем гонять модель в цикле — посчитай, сколько это будет стоить, и скажи мне. Разбор стоит $0.08–0.27: решает число поисков, а его выбирает модель по материалу.

Объясняй, что делаешь. Я хочу понимать систему, а не получить чёрную коробку.

Закрыл пункт — обнови «Статус проверки» в `README.md`: перенеси строку в «проверено исполнением» с указанием, чем именно доказано.
